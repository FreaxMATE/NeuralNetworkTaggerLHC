numpy>=1.24
matplotlib>=3.7
scikit-learn>=1.3
# Prefer a PyTorch version that does not pull CUDA runtime deps by default on Linux
# If you need CUDA, please install via https://pytorch.org with the correct command for your GPU/CUDA.
torch>=2.2,<2.4
pandas>=2.0
scipy>=1.11
